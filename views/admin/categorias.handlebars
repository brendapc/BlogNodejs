<h2>Lista de Categorias:</h2>
<hr>
<a href="/admin/categorias/add"><button class="btn btn-info">Nova Categoria</button></a>
{{#each categorias}}
    <div class="card mt-2">
        <div class="card-body">
            Nome:<h4> {{nome}}</h4>
            <small>Slug: {{slug}}</small>
            <a href="/admin/categorias/edit/{{_id}}"><button class="btn btn-info">editar</button></a>
        </div>
    </div>
{{else}}
{{/each}}